import { RouteLoading } from "@/src/components/RouteLoading";

export default function Loading() {
  return (
    <RouteLoading
      title="Authentication"
      description="Memuat sesi dan context autentikasi."
    />
  );
}
