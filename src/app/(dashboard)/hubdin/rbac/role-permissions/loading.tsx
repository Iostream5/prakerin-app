import { RouteLoading } from "@/src/components/RouteLoading";

export default function Loading() {
  return (
    <RouteLoading
      title="Role Permissions"
      description="Memuat mapping permission per role."
    />
  );
}
